"""The dashboard model tree, split across modules by concern.

The public API is curated in the package root ``__init__`` — import names from
``newrelic_as_code`` directly rather than reaching into these submodules.
"""
